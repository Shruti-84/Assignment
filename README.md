# Morse Code Decoder

This program decodes an encrypted Morse string and prints all possible valid
decodings in alphabetical order.

- User input supported
- No built-in Python functions used
- Implemented using recursion and basic loops

Run using: `python_dev_assignment.py`
